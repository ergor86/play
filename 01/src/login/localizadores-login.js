const loc = {
    usuario : "#form-logar input[type=email]",
    senha : "#form-logar input[type=password]",
    entrar : "#form-logar button",
    modalPrimeiraCompra: {
        tela: '#modal-primeira-compra',
        fechar: 'a.close-reveal-modal',
    },
};

export default loc;